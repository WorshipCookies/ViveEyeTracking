using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using System.Threading;
using LSL;
public class BITalinoConnector : MonoBehaviour
{

    public bool m_RecordECG = true;
    public bool m_RecordEMG = true;
    public bool m_RecordEDA = true;

    private int m_SamplingRate = 100;



    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
